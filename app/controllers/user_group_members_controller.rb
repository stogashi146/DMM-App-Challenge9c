class UserGroupMembersController < ApplicationController
  def create
    # @group = UserGroup.find(params[:id])
    # binding.pry
    # @join = UserGroupMember.new
    # @join.user_id = current_user.id
    # @join.user_group_id = @group.id
    # @join.save
  end
end
